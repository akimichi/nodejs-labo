"use strict";

var expect = require('expect.js');


describe("Math", () => {
  // describe("素数", () => {
  //   var stream = {
  //     cons: (n, thunk) => {
  //     },
      
  //   };
  //    var mark = (array, k, m) => {
  //      var head = array[0]; 
  //      var tail = array.slice(1,array.length);
  //      if(k === m) {
  //        return [0].concat(mark(tail, 1, m));
  //      } else {
  //        return [head].concat(mark(tail, k+1, m));
  //      }
  //    };
  //  var sieve = (array) => {
  //    var head = array[0]; 
  //    var tail = array.slice(1,array.length);

  //    if(head === 0) {
  //      return sieve(tail);
  //    } else {
  //      return [head].concat(sieve(mark(tail, 1, head)));
  //    }
  //  }; 
  //  expect(
  //    sieve([2,3,4,5])
  //  ).to.eql(
  //    [0]
  //  );
  // });
});
